import React from "react";
import { InfoLayout } from "../../styledComponents";

const HomeInfo = ({}) => {
  return (
    <InfoLayout>
      <div className="HomeLogo">Bail</div>
      <div className="HomeContent">
        <p>B로 시작하는 로고를 만들고 싶었다.</p>
        <p>그래서 만들었다.</p>
      </div>
    </InfoLayout>
  );
};

export default HomeInfo;
